Registry-cold ladder, step 2: adds PYTHONPYCACHEPREFIX (lets Python cache compiled bytecode instead of recompiling every boot), model-registry cache still forced empty
run: reg1-s2-pycache-c1  |  model: Qwen3-32B, tp=1, pp=1
RESULT: TOTAL TIME TO READY: 67.4s

Where the time went (largest phases):
- weight load: 26.4s (39.2%)
- CUDA graph capture (pre-building fast-path GPU kernels): 11.2s (16.6%)
- python imports: 14.9s (22.1%)
- model registry resolve (a one-time "what kind of model is this?" check): 6.66s (9.9%)
- warmup / profile run: 3.54s (5.2%)
- engine orchestration: 2.76s (4.1%)
- everything else (interpreter boot, config resolve, network, handshake, kv cache alloc, compile, api server startup, unaccounted): 1.86s (2.8%)

Key takeaways:
- 25.1s of the weight-loading step has no finer breakdown captured (99% of it is one opaque block) — would need more instrumentation to see what's inside it.
- A helper process doesn't start until 23.1s in, so everything it needs to do (imports, GPU setup, weight load) only begins after that — running engine/worker startup at the same time as API server startup is a straightforward win.
- Imports cost 15.0s total when summed across 5 separate Python processes (worst single process: 6.62s). Only the part actually on the critical path counts toward the total, but this is exactly the cost a pre-warmed or forked process would skip.
- The CPU limit was hit (container throttled) for 6.80s across 10 periods during startup. Raising the CPU limit would help, since imports, tensor-format conversion, and compilation are all CPU-bound.
- The "which model architecture is this?" check costs 6.59s here because it runs in a throwaway helper process that pays the full cost of importing vLLM (and therefore PyTorch) just to answer a yes/no question. The answer is cached to disk keyed by a hash of the model's code, so the very next boot on the same cache skips it entirely — measured separately at 14.95s cold vs 0.0002s warm. Pre-populating that cache file before this window starts removes the cost from first boot too.
- That same registry check has no finer breakdown captured either (0% of its interior is covered) — a black box that would need its own instrumentation.
- A 1.49s slice of the warmup/profile-run step is also an opaque block with no finer detail (only 8% of it is broken down further).
- 898ms of network calls sit on the critical path (25ms of that is the actual weight download). If the model is already local, turning on offline mode removes the extra metadata round-trips.

(Condensed summary — full raw trace at runs/reg1-s2-pycache-c1/, original detailed dump preserved in git history.)
