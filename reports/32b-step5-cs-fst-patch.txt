Step 5 — adds the cs_fst patch: tunes the fastsafetensors loader (nogds=True, max_threads=8, bbuf_size_kb=32768)
run: lad2-e-patch-c2  |  model: Qwen3-32B, 1 GPU (TP=1), H100

RESULT: ready in 43.2s

Where the time went (largest phases only):
- Warmup / building CUDA execution graphs: 11.3s (26%)
- Loading model weights: 13.3s (31%)
- Python imports: 10.2s (24%)
- Engine startup bookkeeping: 2.73s (6%)
- Sizing the KV cache (working memory for requests): 2.09s (5%)
- Capturing CUDA graphs: 1.75s (4%)
- Everything else (network lookups, API server startup, etc.): ~1.7s

Key takeaways:
- The last helper process didn't start until 12.2s in, so its imports/GPU-setup work all happens after that point — running things in parallel instead of sequentially is pure upside.
- The weight-loading call itself (11.9s) still has no finer breakdown captured in this trace — would need more instrumentation to see inside it. Same for the warmup/graph-capture step (10.9s, 18% covered) and the KV-cache memory-sizing call (1.94s, 7% covered).
- Python import time across the 3 processes involved totals 10.2s (worst single one: 7.31s in the main server process) — a cost a fully pre-warmed process would still avoid.
- The container was throttled on its CPU limit (16 cores) for 6.05s across 4 moments during startup — imports, tensor-format conversion, and compilation are all CPU-hungry.
- 637ms was spent on network calls (31ms of that the actual weight download, the rest metadata lookups). Disabling Hugging Face's online checks (HF_HUB_OFFLINE=1, TRANSFORMERS_OFFLINE=1) removes these if the model is already stored locally; VLLM_NO_USAGE_STATS=1 also drops one telemetry call.

(Condensed summary — full raw trace at runs/lad2-e-patch-c2/, original detailed dump preserved in git history.)
