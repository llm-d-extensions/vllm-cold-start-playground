Registry-cold ladder, step 4: adds --load-format fastsafetensors (a faster weight loader), model-registry cache still forced empty
run: reg1-s4-fst-c1  |  model: Qwen3-32B, tp=1, pp=1
RESULT: TOTAL TIME TO READY: 52.2s

Where the time went (largest phases):
- weight load: 17.1s (32.8%)
- CUDA graph capture (pre-building fast-path GPU kernels): 10.7s (20.6%)
- python imports: 9.82s (18.8%)
- model registry resolve (a one-time "what kind of model is this?" check): 6.51s (12.5%)
- warmup / profile run: 3.55s (6.8%)
- engine orchestration: 2.49s (4.8%)
- everything else (interpreter boot, config resolve, network, handshake, kv cache alloc, api server startup, unaccounted): 1.86s (3.6%)

Key takeaways:
- A helper process doesn't start until 18.3s in, so everything it needs to do (imports, GPU setup, weight load) only begins after that — running engine/worker startup at the same time as API server startup is a straightforward win.
- 15.8s of the weight-loading step has no finer breakdown captured (99% of it is one opaque block) — would need more instrumentation to see what's inside it. (This is already the faster loader; the win from switching loaders shows up as this whole step shrinking from ~26s in earlier steps to 17.1s.)
- Imports cost 15.0s total when summed across 4 separate Python processes (worst single process: 6.88s). Only the part actually on the critical path counts toward the total, but this is exactly the cost a pre-warmed or forked process would skip.
- The "which model architecture is this?" check costs 6.44s here because it runs in a throwaway helper process that pays the full cost of importing vLLM (and therefore PyTorch) just to answer a yes/no question. The answer is cached to disk keyed by a hash of the model's code, so the very next boot on the same cache skips it entirely — measured separately at 14.95s cold vs 0.0002s warm. Pre-populating that cache file before this window starts removes the cost from first boot too.
- That same registry check has no finer breakdown captured either (0% of its interior is covered) — a black box that would need its own instrumentation.
- The CPU limit was hit (container throttled) for 6.23s across 4 periods during startup. Raising the CPU limit would help, since imports, tensor-format conversion, and compilation are all CPU-bound.
- A 1.46s slice of the warmup/profile-run step is also an opaque block with no finer detail (only 9% of it is broken down further).
- 926ms of network calls sit on the critical path (31ms of that is the actual weight download). If the model is already local, turning on offline mode removes the extra metadata round-trips.

(Condensed summary — full raw trace at runs/reg1-s4-fst-c1/, original detailed dump preserved in git history.)
