Step 2 — adds PYTHONPYCACHEPREFIX=/cache/pycache (lets Python cache compiled bytecode instead of recompiling every boot)
run: lad2-b-pyc-c2  |  model: Qwen3-32B, 1 GPU (TP=1), H100

RESULT: ready in 68.3s

Where the time went (largest phases only):
- Loading model weights: 32.4s (47%)
- Python imports: 16.6s (24%)
- Warmup / building CUDA execution graphs: 10.1s (15%)
- Engine startup bookkeeping: 3.09s (5%)
- Sizing the KV cache (working memory for requests): 1.99s (3%)
- Capturing CUDA graphs: 2.04s (3%)
- Everything else (network lookups, API server startup, etc.): ~1.9s

Key takeaways:
- The weight-loading call itself (31.0s) and the warmup/graph-capture step (9.67s) still have no finer breakdown captured in this trace — would need more instrumentation to see inside them. Same for the KV-cache memory-sizing call (1.84s).
- The last helper process didn't start until 18.4s in, so its imports/GPU-setup work all happens after that point — running things in parallel instead of sequentially is pure upside.
- Python import time adds up to 10.5s total summed across the 4 processes involved (worst single one: 7.30s in the main server process) — already much lower than the pre-caching baseline, but still a cost a pre-warmed process would avoid entirely.
- The container was throttled on its CPU limit (16 cores) for 7.04s across 11 moments during startup — more throttling than before, since more CPU-bound work (imports, tensor-format conversion, compilation) is now packed into less time.
- 894ms was spent on network calls (31ms of that the actual weight download, the rest metadata lookups). Disabling Hugging Face's online checks (HF_HUB_OFFLINE=1, TRANSFORMERS_OFFLINE=1) removes these if the model is already stored locally; VLLM_NO_USAGE_STATS=1 also drops one telemetry call.

(Condensed summary — full raw trace at runs/lad2-b-pyc-c2/, original detailed dump preserved in git history.)
