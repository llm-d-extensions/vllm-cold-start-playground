Step 3 — adds VLLM_WORKER_MULTIPROC_METHOD=fork (worker process inherits the parent's already-loaded Python/torch instead of re-importing from scratch)
run: lad2-c-fork-c3  |  model: Qwen3-32B, 1 GPU (TP=1), H100

RESULT: ready in 63.3s

Where the time went (largest phases only):
- Loading model weights: 32.4s (51%)
- Warmup / building CUDA execution graphs: 11.5s (18%)
- Python imports: 10.5s (17%)
- Engine startup bookkeeping: 2.76s (4%)
- Sizing the KV cache (working memory for requests): 2.06s (3%)
- Capturing CUDA graphs: 2.06s (3%)
- Everything else (network lookups, API server startup, etc.): ~1.8s

Key takeaways:
- The weight-loading call itself (30.8s) still has no finer breakdown captured in this trace (0% of it is broken into sub-steps) — would need more instrumentation to see inside it. Same for the warmup/graph-capture step (11.1s, 20% covered) and the KV-cache memory-sizing call (1.91s, 7% covered).
- The last helper process didn't start until 12.5s in, so its imports/GPU-setup work all happens after that point — running things in parallel instead of sequentially is pure upside.
- 3 Python processes do their own importing, totaling 10.6s (worst single one: 7.61s in the main server process) — that's still a cost a fully pre-warmed process would avoid.
- The container was throttled on its CPU limit (16 cores) for 6.34s across 5 moments during startup — imports, tensor-format conversion, and compilation are all CPU-hungry.
- 709ms was spent on network calls (35ms of that the actual weight download, the rest metadata lookups). Disabling Hugging Face's online checks (HF_HUB_OFFLINE=1, TRANSFORMERS_OFFLINE=1) removes these if the model is already stored locally; VLLM_NO_USAGE_STATS=1 also drops one telemetry call.

(Condensed summary — full raw trace at runs/lad2-c-fork-c3/, original detailed dump preserved in git history.)
