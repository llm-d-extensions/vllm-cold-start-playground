Registry-cold ladder, step 5: adds the cs_fst tuning patch on top of fastsafetensors (disables an unavailable fast-storage path, tunes thread/buffer settings), model-registry cache still forced empty
run: reg1-s5-fsttuned-c2  |  model: Qwen3-32B, tp=1, pp=1
RESULT: TOTAL TIME TO READY: 50.0s

Where the time went (largest phases):
- CUDA graph capture (pre-building fast-path GPU kernels): 11.1s (22.3%)
- weight load: 13.7s (27.4%)
- python imports: 9.65s (19.3%)
- model registry resolve (a one-time "what kind of model is this?" check): 7.14s (14.3%)
- warmup / profile run: 3.76s (7.5%)
- engine orchestration: 2.75s (5.5%)
- everything else (interpreter boot, config resolve, network, handshake, kv cache alloc, api server startup, unaccounted): 1.87s (3.7%)

Key takeaways:
- A helper process doesn't start until 18.9s in, so everything it needs to do (imports, GPU setup, weight load) only begins after that — running engine/worker startup at the same time as API server startup is a straightforward win.
- Imports cost 15.9s total when summed across 4 separate Python processes (worst single process: 7.02s). Only the part actually on the critical path counts toward the total, but this is exactly the cost a pre-warmed or forked process would skip.
- 12.3s of the weight-loading step has no finer breakdown captured (99% of it is one opaque block) — would need more instrumentation to see what's inside it. This is the smallest weight-load time in the ladder, confirming the tuning patch helps on top of the faster loader alone.
- The "which model architecture is this?" check costs 7.05s here because it runs in a throwaway helper process that pays the full cost of importing vLLM (and therefore PyTorch) just to answer a yes/no question. The answer is cached to disk keyed by a hash of the model's code, so the very next boot on the same cache skips it entirely — measured separately at 14.95s cold vs 0.0002s warm. Pre-populating that cache file before this window starts removes the cost from first boot too.
- That same registry check has no finer breakdown captured either (0% of its interior is covered) — a black box that would need its own instrumentation.
- The CPU limit was hit (container throttled) for 6.18s across 9 periods during startup. Raising the CPU limit would help, since imports, tensor-format conversion, and compilation are all CPU-bound.
- A 1.55s slice of the warmup/profile-run step is also an opaque block with no finer detail (only 8% of it is broken down further).
- 691ms of network calls sit on the critical path (34ms of that is the actual weight download). If the model is already local, turning on offline mode removes the extra metadata round-trips.

(Condensed summary — full raw trace at runs/reg1-s5-fsttuned-c2/, original detailed dump preserved in git history.)
