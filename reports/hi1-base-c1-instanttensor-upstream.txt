Cold-start boot using the "instanttensor" weight-loading format, unmodified ("upstream") CUDA-graph-capture behavior — one cycle of a repeated comparison.

run: hi1-base-c1  |  model: Qwen3-32B, TP=1 (single GPU), registry cache already warm
RESULT: TOTAL TIME TO READY: 37.7s

Where the time went (largest phases):
- CUDA graph capture (pre-building fixed-shape GPU execution plans so they run faster later): 11.7s (31.1%)
- loading Python modules: 10.2s (27.1%)
- loading the model's weights onto the GPU: 7.79s (20.7%)
- warmup / profile run (test forward passes to size memory and warm caches): 3.67s (9.8%)
- getting the engine's internal pieces talking to each other: 2.51s (6.7%)
- starting the API server itself: 880ms (2.3%)
- everything else (network checks, config resolve, etc.): well under 1s combined

Biggest single line items: the model's actual weight-copy step (6.33s), the CUDA graph "capture" forward passes (6.37s total across 102 of them), and the engine-startup handshake between the API server and the worker process (2.40s).

Key takeaways:
- One of the four traced processes didn't start until 11.9s in, so everything it needed to do (module loading, GPU setup) only began after that point — running engine/worker startup and API-server startup at the same time, instead of one after another, is a pure win.
- Loading Python modules cost 9.93s total, spread across 3 separate processes (worst single one: 7.11s) — this is the kind of cost a pre-warmed or forked process would avoid.
- 6.33s of the weight-loading step has no finer breakdown captured underneath it (only 3% of that time is accounted for by more detailed sub-steps) — more instrumentation would be needed to see exactly what's happening inside it.
- The container hit its CPU limit and was throttled for 5.41s total across 8 separate periods during startup — imports, file format conversion, and compilation are all CPU-heavy, so a higher CPU limit would likely help.
- 1.46s of the warmup step is similarly unbroken-down (dark span).
- 721ms went to network calls (mostly checking model metadata online); if the model files are already local, setting offline-mode env vars would remove this.
- This run's model-registry check (vLLM figuring out what kind of model it is) was already cached from a previous run, so this total does NOT include that first-time cost (~15s extra on a truly first boot).

(Condensed summary — full raw trace at runs/hi1-base-c1/, original detailed dump preserved in git history.)
