Standalone probe: how much does the "which model is this?" check cost, cold vs. warm cache?
model architecture: Qwen3ForCausalLM  |  5 repeats each
RESULT: registry check costs 14.95s median with an empty cache, 0.000s median with the answer already cached — a difference of 14.95s.

Key takeaways:
- Cold: every one of the 5 repeats spawned a full helper process to answer the question, taking 14.90-15.02s each (median 14.95s).
- Warm: none of the 5 repeats needed to spawn a helper process — the cached answer loads in about 0.1ms.
- Verdict: cold and warm genuinely differ by ~15 seconds, so this check belongs as its own line item in any startup time breakdown, not folded into "imports" or ignored.

(Condensed summary — original detailed dump preserved in git history; raw per-run data in registry-cache-probe.jsonl.)
