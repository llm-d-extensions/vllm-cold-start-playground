Cold-start boot using the "instanttensor" weight-loading format — one cycle of a ladder run establishing the baseline this loader gives.

run: lt1-it-c2  |  model: Qwen3-32B, TP=1 (single GPU), registry cache already warm
RESULT: TOTAL TIME TO READY: 36.9s

Where the time went (largest phases):
- CUDA graph capture: 10.8s (29.3%)
- loading Python modules: 10.2s (27.7%)
- loading the model's weights onto the GPU: 7.70s (20.9%)
- warmup / profile run: 3.79s (10.3%)
- getting the engine's internal pieces talking to each other: 2.54s (6.9%)
- starting the API server: 936ms (2.5%)
- everything else: well under 1s combined

Biggest single line items: the weight-copy step (6.26s), the CUDA graph capture forward passes (5.91s across 102 of them), and the engine-startup handshake (2.42s).

Key takeaways:
- The last of four traced processes started at 12.1s in; running engine/worker startup alongside API-server startup is a pure win.
- Loading Python modules cost 10.3s total across 3 processes (worst one: 7.44s).
- The container was throttled for 7.94s across 8 periods during startup.
- 6.26s of the weight-loading step and 1.54s of the warmup step have no finer breakdown captured underneath them.
- 724ms went to network metadata calls.
- Registry cache was already warm, so this total excludes the ~15s first-boot registry-check cost.

(Condensed summary — full raw trace at runs/lt1-it-c2/, original detailed dump preserved in git history.)
