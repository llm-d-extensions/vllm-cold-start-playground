Step 4 — adds --load-format fastsafetensors (a faster, more parallel way to read weight files off disk)
run: lad2-d-fst-c1  |  model: Qwen3-32B, 1 GPU (TP=1), H100

RESULT: ready in 47.6s

Where the time went (largest phases only):
- Loading model weights: 17.7s (37%)
- Warmup / building CUDA execution graphs: 11.2s (24%)
- Python imports: 10.2s (21%)
- Engine startup bookkeeping: 2.70s (6%)
- Sizing the KV cache (working memory for requests): 2.04s (4%)
- Capturing CUDA graphs: 1.77s (4%)
- Everything else (network lookups, API server startup, etc.): ~1.7s

Key takeaways:
- The weight-loading call itself (16.3s) still has no finer breakdown captured in this trace — would need more instrumentation to see inside it. Same for the warmup/graph-capture step (10.7s, 19% covered) and the KV-cache memory-sizing call (1.89s, 7% covered).
- The last helper process didn't start until 12.2s in, so its imports/GPU-setup work all happens after that point — running things in parallel instead of sequentially is pure upside.
- Python import time across the 3 processes involved totals 10.2s (worst single one: 7.26s in the main server process) — a cost a fully pre-warmed process would still avoid.
- The container was throttled on its CPU limit (16 cores) for 7.79s across 8 moments during startup — imports, tensor-format conversion, and compilation are all CPU-hungry.
- 789ms was spent on network calls (30ms of that the actual weight download, the rest metadata lookups). Disabling Hugging Face's online checks (HF_HUB_OFFLINE=1, TRANSFORMERS_OFFLINE=1) removes these if the model is already stored locally; VLLM_NO_USAGE_STATS=1 also drops one telemetry call.

(Condensed summary — full raw trace at runs/lad2-d-fst-c1/, original detailed dump preserved in git history.)
