Cold-start boot using the "instanttensor" weight-loading format, with the CUDA-graph-capture "hoist" patch applied (moves a per-capture cleanup step out of the inner loop so it only runs once instead of hundreds of times) — one cycle of a repeated comparison.

run: hi1-hoist-c1  |  model: Qwen3-32B, TP=1 (single GPU), registry cache already warm
RESULT: TOTAL TIME TO READY: 32.2s

Where the time went (largest phases):
- loading Python modules: 9.67s (30.0%)
- loading the model's weights onto the GPU: 7.60s (23.6%)
- CUDA graph capture (pre-building fixed-shape GPU execution plans): 6.88s (21.4%)
- warmup / profile run: 3.66s (11.3%)
- getting the engine's internal pieces talking to each other: 2.59s (8.0%)
- starting the API server: 975ms (3.0%)
- everything else: well under 1s combined

Biggest single line items: the weight-copy step (6.21s), the CUDA graph capture forward passes (3.40s across 102 of them — notably lower than the un-patched version), and the engine-startup handshake (2.47s).

Key takeaways:
- Same process-timing pattern as the un-patched run: the last of four processes only started at 11.6s, so overlapping engine/worker startup with API-server startup is still a pure win.
- Loading Python modules cost 9.56s total across 3 processes (worst one: 6.78s).
- The container was throttled for 6.44s across 4 periods during startup.
- 6.21s of the weight-loading step and 1.49s of the warmup step have no finer breakdown captured underneath them.
- 668ms went to network metadata calls.
- Registry cache was already warm, so this total excludes the ~15s first-boot registry-check cost.

(Condensed summary — full raw trace at runs/hi1-hoist-c1/, original detailed dump preserved in git history. See reports/hoist-instanttensor-20260904-010720.txt for the paired base-vs-hoist comparison this run feeds into.)
