Registry-cold ladder, step 3: adds VLLM_WORKER_MULTIPROC_METHOD=fork (worker process inherits already-loaded code instead of re-importing from scratch), model-registry cache still forced empty
run: reg1-s3-fork-c1  |  model: Qwen3-32B, tp=1, pp=1
RESULT: TOTAL TIME TO READY: 60.5s

Where the time went (largest phases):
- weight load: 26.3s (43.6%)
- CUDA graph capture (pre-building fast-path GPU kernels): 10.7s (17.7%)
- python imports: 8.99s (14.9%)
- model registry resolve (a one-time "what kind of model is this?" check): 6.59s (10.9%)
- warmup / profile run: 3.61s (6.0%)
- engine orchestration: 2.50s (4.1%)
- everything else (interpreter boot, config resolve, network, handshake, kv cache alloc, api server startup, unaccounted): 1.68s (2.8%)

Key takeaways:
- 24.9s of the weight-loading step has no finer breakdown captured (100% of it is one opaque block) — would need more instrumentation to see what's inside it.
- A helper process doesn't start until 17.5s in, so everything it needs to do (imports, GPU setup, weight load) only begins after that — running engine/worker startup at the same time as API server startup is a straightforward win.
- Imports cost 14.7s total when summed across 4 separate Python processes (worst single process: 6.49s). Only the part actually on the critical path counts toward the total, but this is exactly the cost a pre-warmed or forked process would skip.
- The "which model architecture is this?" check costs 6.51s here because it runs in a throwaway helper process that pays the full cost of importing vLLM (and therefore PyTorch) just to answer a yes/no question. The answer is cached to disk keyed by a hash of the model's code, so the very next boot on the same cache skips it entirely — measured separately at 14.95s cold vs 0.0002s warm. Pre-populating that cache file before this window starts removes the cost from first boot too.
- That same registry check has no finer breakdown captured either (0% of its interior is covered) — a black box that would need its own instrumentation.
- The CPU limit was hit (container throttled) for 6.30s across 6 periods during startup. Raising the CPU limit would help, since imports, tensor-format conversion, and compilation are all CPU-bound.
- A 1.50s slice of the warmup/profile-run step is also an opaque block with no finer detail (only 9% of it is broken down further).
- 721ms of network calls sit on the critical path (30ms of that is the actual weight download). If the model is already local, turning on offline mode removes the extra metadata round-trips.

(Condensed summary — full raw trace at runs/reg1-s3-fork-c1/, original detailed dump preserved in git history.)
